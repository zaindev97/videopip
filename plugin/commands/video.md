---
description: Plan a video from a plain-language brief and prepare it for `videopip render`
argument-hint: "<what you want, e.g. '8 min video of the 10 gadgets in ./clips, fun tone'>"
---

Use the `videopip` skill to turn this request into a validated VideoPip project, then give me the single command to render it:

$ARGUMENTS
